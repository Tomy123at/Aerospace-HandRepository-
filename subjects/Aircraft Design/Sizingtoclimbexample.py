# C_D = C_D0 + C_L**2/(pi*AR*e)
# C_D0 = f/s
# f es el área parásita, relacionar con Swet en figuras 3.21a, 3.21b, 3.21c
# log10(f) = a + b*log10(Swet)
# log10(Swet) = c + d*log10(W_TO)
# C_D Polares = C_D0 + 1/(pi*AR*e)*C_L**2

# OEI = Motor inoperativo
# (W)